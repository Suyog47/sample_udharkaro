import 'package:flutter/material.dart';
import 'package:material_design_icons_flutter/material_design_icons_flutter.dart';
import 'package:udhaarkaroapp/constants/constants.dart';


const Icon lockOpenIcon = Icon(
  Icons.lock_open,
);

const Icon lockIcon = Icon(
  Icons.lock,
);

const Icon profilePageIconLight = Icon(
  Icons.keyboard_arrow_right,
  color: whiteColor,
);

const Icon profilePageIconDark = Icon(
  Icons.keyboard_arrow_right,
);

const Icon keyUpArrowIcon = Icon(
  Icons.keyboard_arrow_up,
  color: whiteColor,
);

const Icon keyDownArrowIcon = Icon(
  Icons.keyboard_arrow_down,
  color: whiteColor,
);

const Icon backIconLight = Icon(
  Icons.arrow_back,
  color: whiteColor,
  size: 30,
);

const Icon backIconDark = Icon(
  Icons.arrow_back,
  size: 30,
);

const Icon upArrowWhiteIcon = Icon(
  Icons.arrow_upward,
  color: whiteColor,
  size: 20,
);

const Icon upArrowLightBlueIcon = Icon(
  Icons.arrow_upward,
  color: lightBlueColor,
  size: 18,
);

const Icon downArrowWhiteIcon = Icon(
  Icons.arrow_downward,
  color: whiteColor,
  size: 18,
);

const Icon phoneIcon = Icon(
  Icons.call_outlined,
  color: greenColor,
  size: 30,
);

const Icon emailIcon = Icon(
  Icons.email,
  color: blueColor,
  size: 30,
);

const Icon categoryIcon = Icon(
  Icons.account_balance_outlined,
  color: amberColor,
  size: 30,
);

const Icon subCategoryIcon = Icon(
  Icons.category_outlined,
  color: redColor,
  size: 30,
);

const Icon editIcon = Icon(
  Icons.edit,
  color: whiteColor,
);

const Icon qrCodeIcon = Icon(
Icons.qr_code,
color: whiteColor,
);

const Icon shareIconLight = Icon(
  Icons.share,
  color: greenColor,
);

const Icon logoutIconLight = Icon(
  MdiIcons.logout,
  color: whiteColor,
);

const Icon logoutIconDark = Icon(
  MdiIcons.logout,
);

const Icon lockQuestionIcon = Icon(
  MdiIcons.lockQuestion,
  color: skyBlueColor,
  size: 200,
);

const Icon addPhotoIcon = Icon(
  Icons.add_a_photo,
  size: 20,
);

const Icon listIconLight = Icon(
  Icons.list,
  color: whiteColor,
);

const Icon listIconDark = Icon(
  Icons.list,
  color: blackColor,
);

const Icon bellIconLight = Icon(
  MdiIcons.bellRing,
  color: whiteColor,
);

const Icon bellIconDark = Icon(
  MdiIcons.bellRing,
);

const Icon homeIconLight = Icon(
  MdiIcons.home,
  color: whiteColor,
);

const Icon homeIconDark = Icon(
  MdiIcons.home,
);

const Icon accountCircleIconLight = Icon(
  Icons.account_circle,
  color: whiteColor,
);

const Icon accountCircleIconDark = Icon(
  Icons.account_circle,
);

const Icon arrowForwardIconLight = Icon(
  Icons.arrow_forward,
  color: whiteColor,
);

const Icon accountCircleOutlinedIconLight = Icon(
  Icons.account_circle_outlined,
  color: whiteColor,
);

const Icon accountCircleOutlinedIconDark = Icon(
  Icons.account_circle_outlined,
);

const Icon starIcon = Icon(
  Icons.star,
  color: amberColor,
);

const Icon reportIconLight = Icon(
  Icons.report,
  color: whiteColor,
  size: 30,
);

const Icon flashOnIcon =  Icon(
  Icons.flash_on,
  color: whiteColor,
);

const Icon flashOffIcon =  Icon(
  Icons.flash_off,
  color: whiteColor,
);

const Icon menuIcon = Icon(Icons.menu, color: whiteColor,);

const Icon menuDotsIcon = Icon(MdiIcons.dotsVertical, color: whiteColor,);

const Icon categoryDropDownIcon =  Icon(Icons.arrow_drop_down);
